#include <stdio.h>
#include <conio.h>

int main(void) 
{
	int j,m,array[20],sum=0,avg;
	printf("enter the size of the array");
	scanf("%d",&m);
	printf("enter the array elements");
	for(j=0;j<n;j++)
	{
		scanf("%d",&array[j]);
	
	}
	for(j=0;j<n;j++)
	{
		sum=sum+array[j];
	}
	avg=sum/n;
	printf("\n%d",sum);
	printf("\n%d",avg);
  getch();
	return 0;
}

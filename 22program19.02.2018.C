#include<stdio.h>
#include<conio.h>
int large(int arr[], int n)
{
    int s;
    int max = arr[0];  
    for (s = 1; s < n; s++)
    int arr[] = {10, 324, 45, 90, 9808};
    int n = sizeof(arr)/sizeof(arr[0]);
    printf("Large in given array is %d", large(arr, n));
    return 0;
}
